[project]
name = "pratai-newsletter"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "requests==2.34.2",
    "feedparser==6.0.11",
    "langchain-google-genai==4.4.0",
    "langchain-core==1.6.3",
    "python-dotenv==1.2.3",
    "truststore==0.10.4",
]

[tool.vercel]
entrypoint = "api.send_newsletter:handler"
